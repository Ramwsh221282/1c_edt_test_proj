// @strict-types

// Запрос расторгнуть аренду транспорта.
// 
// Параметры:
//  НомерАрендыТранспорта - Строка
// 
// Возвращаемое значение:
//  Структура - Запрос расторгнуть аренду транспорта:
// * НомерАрендыТранспорта - Строка
Функция ЗапросРасторгнутьАрендуТранспорта(НомерАрендыТранспорта)
	Запрос = Новый Структура;
	Запрос.Вставить("НомерАрендыТранспорта", НомерАрендыТранспорта);
	Возврат Запрос;
КонецФункции