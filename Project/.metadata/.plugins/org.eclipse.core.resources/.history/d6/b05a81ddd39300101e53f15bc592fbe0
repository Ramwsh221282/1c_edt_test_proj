&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ИнициализацияНового();
КонецПроцедуры

&НаКлиенте
Процедура НаименованиеПриИзменении(Элемент)
	Объект.Модель = Объект.Наименование;	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьТранспорт(Команда)
	Запрос = МодульПарковТранспортаКлиент.ЗапросДобавитьТранспортВПарк(Объект.Наименование, Объект.Владелец.Код);
	Ответ = МодульПарковТранспортаВызовСервера.ДобавитьТранспортВПарк(Запрос);
	ОповеститьОбИзменении(Ответ.ТранспортСсылка);
	МодульФормКлиент.ЗакрытьБезСохранения(ЭтотОбъект);
КонецПроцедуры

&НаКлиенте
Процедура ИнициализацияНового()
	Если ПустаяСтрока(Объект.Код) Тогда
		Элементы.Статус.ТолькоПросмотр = Истина;
		Элементы.Статус.Доступность = Ложь;
		Объект.Статус = ПредопределенноеЗначение("Перечисление.СтатусыТранспорта.Свободен");
	КонецЕсли;
КонецПроцедуры