// @strict-types

// Создать парк.
// 
// Параметры:
//  Команда - КомандаФормы
&НаКлиенте
Процедура СоздатьПарк(Команда)	
	 Запрос = МодульПарковТранспортаКлиент.ЗапросСоздатьПаркТранспорта(Объект.Наименование, Объект.Адрес);
	 Ответ = МодульПарковТранспортаВызовСервера.ДобавитьТранспортВПак(Запрос);
	 ОповеститьОбИзменении(Ответ.Ссылка);
	 МодульФормКлиент.ЗакрытьБезСохранения(ЭтотОбъект);	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если ПустаяСтрока(Объект.Код) Тогда
				
	КонецЕсли;
КонецПроцедуры